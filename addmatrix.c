    #include<stdio.h>
    int main(){
        int r;
        printf("enter the number of element : ");
        scanf("%d",&r);
        int c;
        printf("Enter the number of elements");
        scanf("%d",&c);
        int arr[r][c]; //r*c total elements
        for(int i=0;i<r;i++){
            for(int j=0;j<c;j++){
            
            }
        }
        return 0;
    }