#include <stdio.h>

// Function definition
int giveZero() {
    return 0;
}

int main() {
    // Call the function and store the result
    int result = giveZero();

    // Print the result
    printf("The function returned: %d\n", result);

    return 0;
}
